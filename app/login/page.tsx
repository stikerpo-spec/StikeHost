"use client";
import {useState} from 'react';import {useRouter} from 'next/navigation';import Link from 'next/link';
export default function Login(){const [f,setF]=useState({email:'',password:''});const [e,setE]=useState('');const r=useRouter();async function go(ev:any){ev.preventDefault();const x=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});const j=await x.json();if(!x.ok)return setE(j.error);r.push('/dashboard')}return <main className="wrap"><div className="nav"><div className="logo">⛏ StikeHost</div><Link href="/">Startseite</Link></div><div className="card" style={{maxWidth:600,margin:'50px auto'}}><h1>Anmelden</h1><form className="form" onSubmit={go}><input className="input" type="email" placeholder="E-Mail" value={f.email} onChange={e=>setF({...f,email:e.target.value})}/><input className="input" type="password" placeholder="Passwort" value={f.password} onChange={e=>setF({...f,password:e.target.value})}/><button className="btn">Anmelden</button>{e&&<div>{e}</div>}</form></div></main>}